var names = ["Philippe", "Phil", "Filip"]

const first = 1;
const second = PEPEGA;

function variations(first, second) {
    if (first == 1) {
        return names[0];
    } else if (first >= 2) {
        return names[1]
    } else (first >= 3)
    return second;
}

variations(4, 0)
console.log(variations)