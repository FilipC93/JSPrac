const ingredients: string[] = [];
const miligramofIngredients: number[] = [];

//?Alternative way of array declaration is:
const myArray: Array<number> = [];
type User = { name: string, isActive: boolean, id: number }
const allUsers: User[] = [];

ingredients.push('sugar');
miligramofIngredients.push(200);
allUsers.push({ name: 'Filip', isActive: false, id: 135131 });